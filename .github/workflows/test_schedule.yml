name: Test schedule

on:
  schedule:
    - cron: "*/5 * * * *"

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print time
        run: |
          echo "Schedule test started"
          echo "UTC time:"
          date -u
          echo "Runner time:"
          date
